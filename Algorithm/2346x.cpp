#include<iostream>
#include<vector>
using namespace std;


int main(void)
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	int n;
	cin >> n;

	vector<int> a(n + 1); //풍선에 적힌 값
	vector<int> prev(n + 1); // 이전 풍선 인덱스
	vector<int> next(n + 1); // 다음 풍선 인덱스



	return 0;
}